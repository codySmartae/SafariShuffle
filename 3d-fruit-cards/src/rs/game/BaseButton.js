/**
 * @author  raizensoft.com
 */
define(
    function () {

        "use strict";

        /**
         * Base class for all buttons
         * @class BaseButton
         * @constructor
         */
        function BaseButton(config) {
            this.init();
        }

        /**
         * Init button
         * @method init
         */
        BaseButton.prototype.init = function () {

            // body...
        };

        /**
         * Init buttons
         * @method init
         */
        BaseButton.prototype.init = function () {

        };

        /**
         * Mouse over handler
         * @method over
         */
        BaseButton.prototype.overHandler = function (e) {

        };

        /**
         * Mouse out handler
         * @method out
         */
        BaseButton.prototype.outHandler = function (e) {

            // body...
        };

        /**
         * Mouse click Handler
         * @method clickHandler
         */
        BaseButton.prototype.clickHandler = function (e) {

            // body...
        };

        return BaseButton;

    });
